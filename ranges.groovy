def  range = 1..10
println(range)

assert range.from == 1 //  means the range start from 2 for real

println(range[1]) // just to make sure that the range is from one to ten but the indexes start from 0